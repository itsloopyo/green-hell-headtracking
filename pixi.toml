[workspace]
name = "green-hell-head-tracking"
version = "1.0.0"
description = "Head tracking mod for Green Hell using CameraUnlock shared library"
channels = ["conda-forge"]
platforms = ["win-64"]

[tasks]
sync = "git submodule update --remote cameraunlock-core"
# Extract net35 MelonLoader DLLs from the vendored zip into libs/ so the build
# resolves them without first running deploy.ps1.
bootstrap-build-deps = "powershell -ExecutionPolicy Bypass -File scripts/bootstrap-build-deps.ps1"
restore = { cmd = "dotnet restore src/GreenHellHeadTracking/GreenHellHeadTracking.csproj", depends-on = ["bootstrap-build-deps"] }
build = { cmd = "dotnet build src/GreenHellHeadTracking/GreenHellHeadTracking.csproj -c Release", depends-on = ["restore"] }
# Checks that every member reference the build emits resolves against the
# assemblies it binds to at runtime. Stub/engine shape drift (a property
# declared as a field, or the reverse) compiles cleanly and only fails in
# game. Needs the game installed; skips and succeeds without it.
verify-refs = { cmd = "powershell -ExecutionPolicy Bypass -File cameraunlock-core/scripts/verify-assembly-refs.ps1 -GameId green-hell -BuildDir src/GreenHellHeadTracking/bin/Release", depends-on = ["build"] }
# Bump vendored mod-loader copies to latest upstream within pinned range.
# Manual; commit the result. CI does not run this.
update-deps = "powershell -ExecutionPolicy Bypass -File scripts/update-deps.ps1"
install = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1 Release", depends-on = ["build"] }
package = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/package-release.ps1", depends-on = ["build"] }
uninstall = "scripts\\uninstall.cmd"
clean = "dotnet clean src/GreenHellHeadTracking/GreenHellHeadTracking.csproj"
release = "powershell -ExecutionPolicy Bypass -File scripts/release.ps1"
show-changelog = "powershell -ExecutionPolicy Bypass -File scripts/show-changelog.ps1"
